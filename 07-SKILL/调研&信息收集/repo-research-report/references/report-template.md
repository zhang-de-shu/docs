# 报告模板

> `repo-research-report` skill 的报告骨架。写报告时严格按本模板章节顺序填。`{{...}}` 是要替换的占位符；引用框里的写作要点写完最终报告时删掉。截图占位符格式、写作规则见 SKILL.md。

---

# {{项目名}} 调研报告

> 调研日期：{{YYYY-MM-DD}}
> 仓库地址：{{URL}}
> 一句话定位：{{做什么的，一句话}}
> 适用人群：零基础，从没用过命令行也能照着用

---

## 一、30 秒看懂这个项目

{{两三句大白话 + 一个生活类比，说清"解决什么问题、为什么需要它"。读完能回答"这东西干嘛的、我有没有可能用得上"。}}

---

## 二、技术原理（算法与原理篇）

> 只讲"凭什么能工作"的算法与原理，不写前后端工程胶水。

### 2.1 它在解决一个什么问题
{{先讲问题再讲方案，让读者先认同"这问题值得解决"。}}

### 2.2 核心思路（一句话 + 类比）
{{一句话核心机制 + 生活类比。例："导入时先消化成结构化笔记，而不是用时才翻原料堆"——像读书边读边做卡片笔记。}}

### 2.3 算法 / 原理详解
{{原理篇主体，分小步展开。有论文先一句话讲核心 idea 再展开，公式下紧跟"翻译成人话…"；有源码用真实数据结构佐证但不贴大段代码；术语首次出现即解释。项目算法含量低（纯工具）则如实说"本身无复杂算法，原理在组合了 X"，把 X 讲清。}}

### 2.4 关键流程（文字流程图）
```
输入 → {{阶段1}} → {{阶段2}} → {{阶段3}} → 输出
```
每阶段一两句说明在干嘛、为何必要。

### 2.5 和同类比，它妙在哪
{{对比 1-2 个竞品，讲"为什么这个设计选择让它更 X"，不罗列功能。}}

### 2.6 局限与适用边界
{{如实说不擅长什么、什么场景别用。}}

---

## 三、使用说明（手把手篇）

> 假设读者从没碰过命令行。每步写：在哪操作 / 点什么输什么 / 成功长什么样 / 截图占位符。

### 3.0 开始前准备
- 一台 {{Mac/Windows/Linux}} 电脑
- {{是否需联网、账号或 API key（去哪注册）、要装什么软件及版本}}

### 3.1 打开终端
- Mac：应用程序→实用工具→终端
- Windows：搜 PowerShell（管理员身份运行）

![截图：打开后的终端](待补充截图)
> 截图说明：打开终端后，{{黑/蓝底}}窗口，提示符类似 `{{用户名@电脑名 ~ %}}`，能看到闪烁光标。

### 3.2 获取项目代码
1. 下载 ZIP（最简）：去 `{{URL}}`，点绿色 `Code`→`Download ZIP`→解压。
2. 或 git 克隆：`git clone {{clone 地址}}`（没 git 先装）。

![截图：GitHub Code 下拉菜单](待补充截图)
> 截图说明：Code 按钮点开后的下拉，要能看到 `Download ZIP`。

### 3.3 进入项目文件夹
```
cd {{项目文件夹名}}
```
说明 `cd` 含义、按回车、成功后提示符路径变成项目文件夹名。

![截图：cd 后的终端](待补充截图)
> 截图说明：提示符变成 `... {{文件夹名}} %`，路径已切到项目。

### 3.4 安装运行环境
按项目实际写（Python: `pip install -r requirements.txt`；Node: `npm install`；Docker: `docker compose up -d`）。每条命令下解释在干嘛、屏幕会怎样、出现什么算成功。

![截图：依赖安装成功](待补充截图)
> 截图说明：底部出现 `Successfully installed`/`added xxx packages`，包含那行。

### 3.5 配置（如需要）
- 去哪注册拿 key：{{地址，点哪个按钮}}
- key 长啥样：{{样例}}
- 写到哪：{{文件路径}}，改成什么样子

![截图：配置文件填写](待补充截图)
> 截图说明：编辑器打开 `{{配置文件}}`，填上 key 的样子（**key 涂掉再截图**）。

### 3.6 运行
```
{{运行命令}}
```
解释启动什么、屏幕反应、什么算成功（如 `Running on http://localhost:xxxx`）。

![截图：启动成功](待补充截图)
> 截图说明：打印出 `{{成功标志}}`，包含那行。

### 3.7 验证跑起来了
{{最小验证动作，如浏览器开 `http://localhost:xxxx` 看到某界面 / 跑条测试命令看到某输出}}。

![截图：验证成功](待补充截图)
> 截图说明：{{验证动作后}}出现 `{{成功标志}}`。

### 3.8 常见报错与解决
列 3-5 个最可能踩的，每个：报错原文→原因→怎么解。例：
- `command not found: python` → 没装或没进 PATH → 去 {{地址}} 装，重开终端。
- `permission denied` → 权限不够 → 前加 `sudo` 或改权限。

---

## 四、待补充截图清单

| 编号 | 位置 | 要截什么 | 必须包含 |
|------|------|---------|---------|
| 1 | 3.1 打开终端 | 终端刚打开 | 提示符、光标 |
| 2 | 3.2 下载代码 | Code 下拉菜单 | Download ZIP |
| ... | ... | ... | ... |

---

## 五、参考来源
- 仓库：{{URL}}
- 官方文档：{{URL}}
- 论文：{{标题 + arXiv/链接}}
- 其他：{{博客/对比文等}}

---

## 六、本报告说明
- 关键假设：{{选了哪个候选仓、缺失资料用什么替代}}
- 资料偏薄章节：{{哪节不够、建议补什么}}
- 截图待补：共 {{N}} 处，见第四节。
